"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { Button, Card, Field, SectionTitle, inputClass } from "@/components/ui";

export function AuthPage({ mode }: { mode: "login" | "signup" }) { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = (event: React.FormEvent) => { event.preventDefault(); if (!email.includes("@") || password.length < 8 || (mode === "signup" && password !== confirm)) { setError(mode === "signup" && password !== confirm ? "Passwords must match." : "Enter a valid email and an 8-character password."); return; } setLoading(true); setTimeout(() => { localStorage.setItem("thinkflow-user", email); router.push(mode === "signup" ? "/onboarding" : "/dashboard"); }, 450); }; const signup = mode === "signup"; return <main className="grid min-h-screen place-items-center bg-canvas p-5"><Card className="w-full max-w-md p-6 sm:p-8"><SectionTitle eyebrow="ThinkFlow" title={signup ? "Create your account" : "Welcome back"} copy={signup ? "Start focused pattern-recognition practice." : "Continue your deliberate practice."}/><form onSubmit={submit} className="space-y-5"><Field label="Email"><input value={email} onChange={e=>setEmail(e.target.value)} className={inputClass} type="email" autoComplete="email" /></Field><Field label="Password" hint="At least 8 characters"><input value={password} onChange={e=>setPassword(e.target.value)} className={inputClass} type="password" autoComplete={signup ? "new-password" : "current-password"}/></Field>{signup && <Field label="Confirm password"><input value={confirm} onChange={e=>setConfirm(e.target.value)} className={inputClass} type="password" autoComplete="new-password"/></Field>}{error && <p role="alert" className="text-sm text-danger">{error}</p>}<Button loading={loading} className="w-full" type="submit">{signup ? "Create account" : "Continue"}</Button></form><p className="mt-6 text-center text-sm text-muted">{signup ? "Already have an account?" : "New to ThinkFlow?"} <Link className="text-accent hover:underline" href={signup ? "/login" : "/signup"}>{signup ? "Log in" : "Create an account"}</Link></p></Card></main>; }
